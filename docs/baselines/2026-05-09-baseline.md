# 基线快照 — 2026-05-09

> Week 1 末抓取的项目健康度基线指标，用于 6-8 周重构后对比改进效果。
> 对应 commit: `6df4e88a`（Week 1.6 完成态）

---

## 测试覆盖率

通过 `pnpm test:coverage` 在 commit `6df4e88a` 抓取。

| 指标 | 数值 |
|---|---|
| **All files - 语句覆盖** | **53.67%** |
| All files - 分支覆盖 | 41.63% |
| All files - 函数覆盖 | 51.54% |
| All files - 行覆盖 | 54.32% |
| 测试文件数 | 48 |
| 测试用例数 | 181（全绿） |

### 关键模块覆盖率

| 模块 | 语句覆盖 | 备注 |
|---|---|---|
| `src/credits/` | ~78% | 核心路径较好 |
| `src/payment/` | 100% (types.ts) | 但 `provider/zpay.ts` 仅 41% |
| `src/payment/provider/zpay.ts` | **41.27%** | webhook 入口未测 → Week 3 补 |
| `src/lib/auth.ts` | **3.03%** | 几乎无测试 → Week 3 补 |
| `src/lib/admin.ts` | **0%** | 无测试 |
| `src/storage/` | 69.56% | 中等 |
| `src/newsletter/` | 63.88% | 中等 |
| `src/i18n/messages.ts` | 42.85% | 单 locale 后简化 |
| `src/mail/templates/` | 33.33% | 仅渲染层覆盖 |
| `src/mail/provider/resend.ts` | 0% | 无 mock 测试 |

### 8 周后目标

- All files 语句覆盖 ≥ 80%（Week 3 补 webhook + 真 DB 集成测试 + Week 4 状态层重构）
- 0% 模块清零（auth、admin、resend、storage/types）

---

## 构建产物 (Bundle)

通过 `pnpm build` 在 production 模式抓取。

| 指标 | 数值 |
|---|---|
| `.next/static` 总大小 | **34 MB** |
| `.next/static/chunks` JS | 27 MB |
| `.next/static/media` 静态资源 | 7.5 MB |
| `.next` 全部产物 | 948 MB |

### 最大 JS 块（疑似大依赖）

| 大小 | 文件 |
|---|---|
| 788 KB | `chunks/7dd7c79defb4e72a.js` |
| 764 KB | `chunks/b0eecb5bd6c00791.js` |
| 684 KB | `chunks/05800e79aa608697.js` |
| 616 KB | `chunks/624597ca0971ecca.js` |
| 612 KB | `chunks/a467aac537587e1e.js` |
| 264 KB | `chunks/4fd75a06cf269954.js` |
| 260 KB | `chunks/25ca2ee445b54b9b.js` |
| 220 KB | `chunks/5010cec8634bc196.js` |

> Top 5 chunks ≈ 3.46 MB。Week 6 RSC 化 + 重复依赖清理后预计降 30%+。

### 8 周后目标

- `.next/static` 总大小 ≤ 25 MB（-26%）
- 单个 JS chunk ≤ 500 KB
- 重复依赖清零（framer-motion / motion、@radix-ui/* / radix-ui、use-intl）

---

## 死代码分析（knip）

通过 `pnpm knip` 抓取（commit `6df4e88a`）。

| 分类 | 数量 |
|---|---|
| Unused files | **285** |
| Unused dependencies | 39 |
| Unused devDependencies | 2 |
| Unlisted dependencies | 2 |
| Unused exports | 151 |
| Unused exported types | 64 |
| Unused exported enum members | 5 |
| Duplicate exports | 2 |

### 待重点清理

- `src/components/tailark/*` — 模板残留组件库
- `src/credits/types.ts:64` `CreditPackagePrice` 接口（实为内部类型）
- `src/lib/rate-limit.ts` 重复导出 `applyRateLimit | checkRateLimit`
- `src/routes.ts` enum members `About / Contact / Waitlist / Changelog / Roadmap`
- `src/payment/types.ts` 的 `Customer / Payment / PaymentScene` 等 8 个未使用接口

### 8 周后目标

- Unused files ≤ 50（-82%）
- Unused dependencies = 0
- Duplicate exports = 0

---

## 源码体量

通过 `find` + `wc -l` 抓取。

| 指标 | 数值 |
|---|---|
| TS/TSX 源文件总数 | **767** |
| TS/TSX 源文件总行数 | **86,132** |
| MDX 内容文件 (zh) | 52 |
| `messages/zh.json` 行数 | 1,851 |

### 8 周后目标

- 文件数 ≤ 700（-9%）— 通过组件合并 / 删 docs / 删 newsletter / 合并 image utils
- 总行数 ≤ 70,000（-19%）

---

## 路由数量

通过 `pnpm build` 输出抓取。

| 类型 | 数量 |
|---|---|
| 页面路由（`src/app/[locale]/...`） | 33 |
| API 路由（`src/app/api/...`） | 14 |
| **总计** | 47 |

### 计划清理

Week 7-8 移除：`/about`, `/changelog`, `/contact`, `/waitlist`, `/help`, `/help/[slug]`, `/docs/[[...slug]]`, `/blog/*` 部分（保留 SEO blog 主路径）

---

## CI / 质量工程现状

| 项目 | 状态 |
|---|---|
| GitHub Actions workflows | 1 (`test.yml`) |
| CI 跑 lint? | ❌ 否 |
| CI 跑 typecheck? | ❌ 否 |
| CI 跑 build? | ❌ 否 |
| CI 跑 test? | ✅ 是（但 `continue-on-error: true`，不阻断） |
| Pre-commit hook? | ❌ 无（无 husky/lefthook） |
| 错误监控 (Sentry)? | ❌ 无 |
| Feature flag? | ❌ 无 |
| 真 DB 集成测试? | ❌ 全 mock |
| E2E 测试? | ❌ 无（无 Playwright） |
| Coverage 阈值? | ❌ 无 |
| Conventional Commits? | ❌ 无（commit 风格手动维护） |

### 8 周后目标

- CI 跑 lint + typecheck + build + test，全绿才能合并 ✅
- Pre-commit hook 阻断 lint/type error ✅
- Sentry 接入（待 DSN）
- Playwright E2E 5 条核心路径
- Coverage 80% 阈值
- Conventional Commits + 自动 CHANGELOG

---

## 已知技术债快照

按 Week 1 终审报告整理（CRITICAL 已修，HIGH 待修）：

### CRITICAL 已修 ✅
- ✅ 文档失同步 (Week 1.1)
- ✅ holdCredits 并发缝隙 (Week 1.2)
- ✅ Lifetime webhook 月度积分错发 (Week 1.3)
- ✅ Pro 订阅幻觉重构 (Week 1.6)
- ⏸️ Sentry 接入（待 DSN，Week 1.4 暂缓）

### HIGH 待修 ⏳
- busyProjectId 防护失效（Week 2）
- 双 store currentProjectId 不同步（Week 2）
- MessageItem.tsx 重复代码（Week 2）
- Admin bypass 无审计（Week 2）
- CI 不跑 lint/typecheck/build（Week 3）
- 无 E2E、无真 DB 集成测试（Week 3）
- Hold 释放路径 race condition（Week 2）
- Recovery hook 失败误判（Week 2）
- Webhook 端点缺限流（Week 2）
- Referral 重复触发去重（Week 2）

### 长期重构 ⏳
- 状态管理三处分裂（Week 4）
- 39/39 全 `'use client'` 组件未 RSC 化（Week 6）
- localStorage 持久化 base64 风险（Week 4）
- 重复 image utils 三处（Week 6）

---

## 抓取脚本（可重复运行）

```bash
# 1. Coverage
pnpm test:coverage 2>&1 | grep -E "^All files"

# 2. Bundle
du -sh .next/static
find .next/static/chunks -name "*.js" -exec du -h {} + | sort -hr | head -10

# 3. Knip
pnpm knip 2>&1 | grep -E "^Unused|^Duplicate"

# 4. Source size
find src -name "*.ts" -o -name "*.tsx" | xargs wc -l | tail -1
find src -name "*.ts" -o -name "*.tsx" | wc -l
```

后续每周做完后跑同样脚本，记录到 `docs/baselines/YYYY-MM-DD-{week}.md`。
